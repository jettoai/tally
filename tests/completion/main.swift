import Foundation

// Prints the completion script so a real zsh can be asked what it offers. The script is a constant
// inside the CLI (TallyCLI/Completion.swift); everything else in this suite is that text, running.
print(tallyCompletionZsh)
